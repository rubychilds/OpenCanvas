This project bundles a live OpenCanvas design canvas with an MCP server. For any request that involves visually designing, previewing, or iterating on UI, prefer the OpenCanvas MCP tools (add_components, update_styles, get_screenshot, get_jsx, etc.) over writing React files. Write React files only when the user explicitly asks for a file, or when the task is logic/state/routing rather than visual layout.