node_modules/
dist/
build/
coverage/
.vite/
.turbo/
*.tsbuildinfo

# Playwright
test-results/
playwright-report/
.e2e-artifacts/

# project data files written at runtime
.opencanvas.json

# MCP config files are user-specific; they're written by `opencanvas init` on
# whoever's machine. Don't commit them -- users pick the one they want.
.mcp.json
.cursor/mcp.json
.vscode/mcp.json

# OS / IDE
.DS_Store
.vscode/
.idea/

# env + logs
.env
.env.*
!.env.example
*.log

# Claude / local tooling caches
.claude/cache/

# Local research clones — external repos pulled in for reading, not vendored.
# Keep their own .git intact and out of our tree.
penpot/
